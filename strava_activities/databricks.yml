# This is a Databricks asset bundle definition for strava_activities.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: strava_activities
  uuid: 677dca7e-11f7-4a81-b45c-3c362f59a6d3

artifacts:
  python_artifact:
    type: whl
    build: uv build --wheel

include:
  - resources/*.yml
  - resources/*/*.yml

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://dbc-8db3bb0f-f960.cloud.databricks.com
      root_path: /Workspace/Users/rajnil94.guha@gmail.com/dev/.bundle/${bundle.name}/${bundle.target}
    presets:
      name_prefix: dev_${workspace.current_user.short_name}
      source_linked_deployment: false

  prod:
    mode: production
    workspace:
      host: https://dbc-8db3bb0f-f960.cloud.databricks.com
      # We explicitly deploy to /Workspace/Users/rajnil94.guha@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Users/rajnil94.guha@gmail.com/strava/prod/.bundle/${bundle.name}/${bundle.target}
    permissions:
      - user_name: rajnil94.guha@gmail.com
        level: CAN_MANAGE
